"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var schema_1 = require("./schema");
var rules = require("./rules");
exports.DiffRule = rules;
function diff(oldSchema, newSchema, rules) {
    if (rules === void 0) { rules = []; }
    var changes = schema_1.diffSchema(oldSchema, newSchema);
    return rules.reduce(function (prev, rule) {
        return rule({
            changes: prev,
            oldSchema: oldSchema,
            newSchema: newSchema,
        });
    }, changes);
}
exports.diff = diff;
//# sourceMappingURL=index.js.map