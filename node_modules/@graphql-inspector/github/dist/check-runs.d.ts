import { Context } from 'probot';
import { Annotation, CheckConclusion } from './types';
export declare function start({ context, owner, repo, sha, }: {
    sha: string;
    context: Context;
    owner: string;
    repo: string;
}): Promise<string>;
export declare function annotations({ context, url, annotations, title, summary, }: {
    url: string;
    annotations: Annotation[];
    context: Context;
    title: string;
    summary: string;
}): Promise<void>;
export declare function complete({ context, url, conclusion, }: {
    url: string;
    context: Context;
    conclusion: CheckConclusion;
}): Promise<void>;
